import { validator } from './validators';

describe('Validator module testing', () => {
    it('validator fn can validate a name', () => {

    });

    it('validator fn can validate an email', () => {

    });
})