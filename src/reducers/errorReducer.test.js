import { errorReducer } from './errorReducer';
import * as actions from '../actions'

describe('errorReducer', () => {
  it.skip('should return the initial state', () => {

  });

  it.skip('should return the state with an error message', () => {

  });
});