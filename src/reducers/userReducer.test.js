import { userReducer } from './userReducer';
import * as actions from '../actions';

describe('userReducer', () => {
  it.skip('should return the initial state', () => {

  });

  it.skip('should return the state with a name and id', () => {

  });
});