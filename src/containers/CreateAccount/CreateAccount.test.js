import CreateAccount, { mapStateToProps, mapDispatchToProps} from './CreateAccount';
import { shallow } from 'enzyme';
import { updateError } from '../../actions';

describe('CreateAccount', () => {
  describe('CreateAccount component', () => {
    it.skip('should match the snapshot', () => {

    });

    it.skip('should have a default state', () => {

    });

    it.skip('should handleChange from the input form', () => {

    });

    it.skip('should handleSubmit', () => {

    });
  });

  describe('mapStateToProps', () => {
    it.skip('should return an object with an error status', () => {

    });
  });

  describe('mapDispatchToProps', () => {
    it.skip('should call dispatch when using a function from mapDispatchToProps', () => {

    });
  });
});